// You don't need care about this file.
// Production use /config.js to get runtime environments so that we can pass envs to project when exec docker run or docker compose.
// Specific envs will be translated to config.js by env.sh, see Dockerfile and env.sh
// Local development use .env file as your environments, see env.js.
// In local development, This file is also used to prevent local development error if can't find /config.js.
